{
  "name": "@chidiwilliams/loxjs",
  "version": "1.0.0",
  "description": "Lox interpreter",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "test": "tap --ts src/**.test.ts",
    "prepare": "npm run build",
    "postversion": "git push && git push --tags"
  },
  "author": "Chidi Williams",
  "license": "ISC",
  "devDependencies": {
    "@types/tap": "^15.0.6",
    "prettier": "^2.5.1",
    "tap": "^15.2.2",
    "ts-node": "^10.6.0"
  }
}
